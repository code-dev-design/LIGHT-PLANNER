# نشر A2Z Lighting Demo من GitHub إلى Render

هذه النسخة جاهزة للنشر كتطبيق ويب. العميل يفتح الرابط من المتصفح ولا يحتاج إلى Python أو مكتبات على جهازه.

## 1) رفع المشروع إلى GitHub

1. أنشئ Repository جديداً على GitHub.
2. ارفع **محتويات هذا المجلد كلها إلى جذر المستودع**، بحيث يظهر `app.py` و`render.yaml` مباشرة في الصفحة الأولى للمستودع.
3. تأكد أن الملفات التالية موجودة:
   - `app.py`
   - `requirements.txt`
   - `.python-version`
   - `render.yaml`
   - مجلد `static`

## 2) النشر على Render

1. افتح Render وسجل الدخول بحساب GitHub.
2. اختر **New +** ثم **Blueprint**.
3. اختر مستودع GitHub الذي رفعت إليه المشروع.
4. Render سيقرأ ملف `render.yaml` تلقائياً.
5. اضغط **Apply** أو **Deploy Blueprint**.
6. بعد اكتمال البناء سيظهر رابط عام شبيه بـ:
   `https://a2z-lighting-vector-demo.onrender.com`

## إعدادات النشر المضافة

- Python 3.13 من خلال `.python-version`.
- تثبيت `PyMuPDF` من `requirements.txt`.
- تشغيل الخادم على `0.0.0.0` والمنفذ الذي يرسله Render في `PORT`.
- فحص صحة على `/api/health`.
- تعطيل فتح المتصفح تلقائياً على السيرفر.
- حفظ الملفات المرفوعة مؤقتاً فقط؛ لا توجد قاعدة بيانات أو تخزين دائم في نسخة الديمو.

## ملاحظات مهمة

- ملفات المستخدم المرفوعة مؤقتة وقد تختفي عند إعادة تشغيل الخدمة أو إعادة نشرها.
- النسخة المجانية قد تدخل وضع السكون بعد عدم الاستخدام، لذلك أول فتح بعد فترة قد يتأخر قليلاً.
- لا تستخدم ملفات سرية أو حساسة جداً في الديمو العام.
- دعم الديمو الحالي للـPDF المتجهي. DXF وDWG ما زالا خارج نطاق هذه النسخة.
